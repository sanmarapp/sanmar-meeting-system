{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema=prisma/schema.prisma && cd apps/backend && /app/node_modules/.bin/nest build"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=prisma/schema.prisma && npm run start:prod -w apps/backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
