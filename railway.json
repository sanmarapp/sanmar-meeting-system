{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build -w apps/backend && npx prisma generate"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npx prisma db seed && npm run start:prod -w apps/backend",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
